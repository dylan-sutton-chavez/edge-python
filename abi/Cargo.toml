[package]
name = "wasm-abi"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Plugin ABI wire-format constants shared by the Edge Python compiler and the wasm-pdk. no_std, zero deps."

[lib]
crate-type = ["rlib"]
doctest = false
test = false
