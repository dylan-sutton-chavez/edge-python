name: CI / CD

# Single pipeline for the monorepo, each job's logic lives in a composite action, this file wires the graph.

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compiler-check:
    name: Compiler / Lint and Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/compiler
        with:
          mode: check

  js-lint:
    name: JS / Deno Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/js
        with:
          mode: lint

  compiler:
    name: Compiler / Test and Release
    needs: [compiler-check, js-lint]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/compiler
        with:
          mode: build
          github-token: ${{ github.token }}

  js:
    name: JS / Deno Test
    needs: [compiler]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/js
        with:
          mode: test

  # Strips web/src to plain JS, the CDN can't serve TypeScript. Independent of the compiler build.
  js-build:
    name: JS / Build
    needs: [js-lint]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/js
        with:
          mode: build

  web:
    name: Web / ${{ matrix.capability }}
    needs: [js]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        capability: [dom, network, storage, time]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/web
        with:
          capability: ${{ matrix.capability }}

  std:
    name: Std / ${{ matrix.package }}
    needs: [js]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package: [json, re, math, struct, test]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/std
        with:
          package: ${{ matrix.package }}

  # std packages as native plugin cdylibs for the in-process engine, shipped to the CDN and tag Releases.
  native:
    name: Native std / ${{ matrix.package }} (${{ matrix.target }})
    needs: [compiler-check]
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        package: [json, re, math, struct]
        target: [aarch64-unknown-linux-gnu, x86_64-unknown-linux-gnu, aarch64-apple-darwin, x86_64-apple-darwin]
        include:
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
          - target: aarch64-apple-darwin
            runner: macos-15
          - target: x86_64-apple-darwin
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/native
        with:
          package: ${{ matrix.package }}
          target: ${{ matrix.target }}
          github-token: ${{ github.token }}

  # Clippy and check once with no matrix, gates the release build.
  cli-lint:
    name: CLI / Lint and Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/cli
        with:
          mode: lint

  # Heavy release build per target, starts as soon as cli-lint is green.
  cli-release:
    name: CLI / Release (${{ matrix.target }})
    needs: [cli-lint]
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
          - target: aarch64-apple-darwin
            runner: macos-15
          - target: x86_64-apple-darwin
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/cli
        with:
          mode: release
          target: ${{ matrix.target }}

  cli-test:
    name: CLI / Test / ${{ matrix.suite }}
    needs: [web, std, native, cli-release, js-build]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        suite: [engine, network, actors, skill]
    steps:
      - uses: actions/checkout@v6
      - name: Download web/dist (JS build output)
        uses: actions/download-artifact@v8
        with:
          name: web_dist
          path: web/dist
      - uses: ./.github/actions/cli
        with:
          mode: test
          suite: ${{ matrix.suite }}

  cdn:
    name: Cloudflare Upload (CDN)
    needs: [cli-test, native, js-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/cdn-deploy
        with:
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  # Builds on every run (PR gate) and uploads the static export for docs-deploy.
  docs-build:
    name: Docs / Build
    needs: [cli-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs
        with:
          mode: build

  # Tail of the docs build and deploy chain, main pushes only.
  docs-deploy:
    name: Docs / Deploy
    needs: [docs-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs
        with:
          mode: deploy
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
