name: edge-python-afl

services:
  fuzzer:
    build:
      context: ../..
      dockerfile: fuzz/Dockerfile
    restart: unless-stopped
    volumes:
      - findings:/app/fuzz/out

    environment:
      - JOBS
      - DURATION
      - FRESH
      - TIMEOUT_MS
      - AFL_NO_AFFINITY=1 # containers hide cores; let scheduler place

volumes:
  findings:
