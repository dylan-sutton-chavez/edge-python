[
  {
    "src": "b = body()\nfor name, color in [('A','#111'),('B','#222'),('C','#333')]:\n    box = create_element('div')\n    set_attribute(box, 'class', 'box')\n    set_attribute(box, 'style', f'background:{color}')\n    label = create_element('span')\n    set_text(label, f'{name} {color}')\n    append_child(box, label)\n    append_child(b, box)\nprint(len([h for h in children(b).split(',') if h]))",
    "output": ["3"]
  },
  {
    "src": "btn = create_element('button')\nset_text(btn, 'Clicked 0 times')\nappend_child(body(), btn)\nbind_event(btn, 'click', 'click')\nclick(btn)\nev = receive()\nprint('\"msg\":\"click\"' in ev)\nset_text(btn, 'Clicked 1 time')\nprint(get_text(btn))",
    "output": ["True", "Clicked 1 time"]
  },
  {
    "src": "grid = create_element('div')\nappend_child(body(), grid)\nadd = create_element('button')\nappend_child(body(), add)\nbind_event(add, 'click', 'add')\nclick(add)\nev = receive()\nif '\"msg\":\"add\"' in ev:\n    swatch = create_element('div')\n    append_child(grid, swatch)\nprint(tag_name(last_child(grid)))",
    "output": ["div"]
  },
  {
    "src": "inp = create_element('input')\nappend_child(body(), inp)\nset_value(inp, '#ff0080')\nprint(get_value(inp))\nset_value(inp, '')\nprint(get_value(inp) == '')",
    "output": ["#ff0080", "True"]
  },
  {
    "src": "el = create_element('div')\nappend_child(body(), el)\nh = animate(el, '[{\"opacity\":0},{\"opacity\":1}]', '{\"duration\":30}', 'done')\nev = receive()\nprint('\"msg\":\"done\"' in ev, '\"ok\":true' in ev, f'\"animation_handle\":{h}' in ev)",
    "output": ["True True True"]
  },
  {
    "src": "el = create_element('div')\nappend_child(body(), el)\nwith batch():\n    set_text(el, 'a')\n    print(get_text(el))\nprint(get_text(el))",
    "output": ["", "a"]
  },
  {
    "src": "el = create_element('div')\nappend_child(body(), el)\nwith batch():\n    set_text(el, 'x')\n    set_attribute(el, 'title', 't1')\n    add_class(el, 'boxed')\n    print(pending())\nprint(pending())\nprint(get_text(el))\nprint(get_attribute(el, 'title'))\nprint(get_attribute(el, 'class'))",
    "output": ["3", "0", "x", "t1", "boxed"]
  },
  {
    "src": "el = create_element('span')\nappend_child(body(), el)\nwith batch():\n    set_text(el, 'first')\n    set_text(el, 'second')\nprint(get_text(el))",
    "output": ["second"]
  },
  {
    "src": "flush()\nprint(pending())",
    "output": ["0"]
  },
  {
    "src": "b = body()\nel = create_element('div')\nappend_child(b, el)\ntry:\n    with batch():\n        set_text(el, 'nope')\n        raise ValueError('boom')\nexcept ValueError:\n    pass\nprint(get_text(el))\nprint(pending())",
    "output": ["", "0"]
  },
  {
    "src": "b = body()\nel = create_element('div')\nappend_child(b, el)\nwith batch():\n    with batch():\n        set_text(el, 'n')\n    print(get_text(el))\nprint(get_text(el))",
    "output": ["", "n"]
  },
  {
    "src": "b = body()\nel = create_element('div')\nappend_child(b, el)\nwith batch():\n    remove(el)\nprint(len([h for h in children(b).split(',') if h]))",
    "output": ["0"]
  },
  {
    "src": "inp = create_element('input')\nappend_child(body(), inp)\nwith batch():\n    set_value(inp, '42')\n    set_checked(inp, True)\nprint(get_value(inp))\nprint(get_checked(inp))",
    "output": ["42", "True"]
  },
  {
    "src": "apply_batch([['query', 'div']])",
    "error": "not batchable"
  }
]
