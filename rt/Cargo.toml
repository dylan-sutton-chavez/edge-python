[package]
name = "rt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Cooperative no_std executor and Park frontier. Portable core, the host supplies the reactor."

[lib]
crate-type = ["rlib"]
doctest = false

[[test]]
name = "tests"
path = "tests.rs"

[dependencies]
