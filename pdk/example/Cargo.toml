[package]
name = "slugify-mod"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]
test = false
doctest = false

[dependencies]
wasm-pdk = { path = ".." }
lol_alloc = { workspace = true }
